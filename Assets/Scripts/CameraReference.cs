using UnityEngine;

[CreateAssetMenu(fileName = "New Camera Reference", menuName = "Battle of Heroes / References / Camera Reference")]
public class CameraReference : ScriptableObject
{
    public Camera camera;
}